language: java
jdk:
  - openjdk11
install: true
env:
  - DOCKER_PASSWORD=$DOCKER_CREDENTIAL
services:
  - docker
stages:
  - name: test
  - name: deploy
    if: branch = master
jobs:
  include:
    - stage: test
      script:
        - mvn install
    - stage: deploy
      script:
        - mvn deploy
cache:
  directories:
    - $HOME/.m2
